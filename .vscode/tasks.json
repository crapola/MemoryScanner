{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "SCons Build",
      "command": "scons",
      "type": "shell",
      "group": "build",
      "problemMatcher": "$msCompile",
      "options": {
        "env": {
          "path": "${env:path}; F:/Python/Scripts/;"
        },
        "cwd": "${workspaceFolder}/test_program"
      }
    }
  ]
}